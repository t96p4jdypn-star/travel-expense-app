同じプロジェクトの `AGENTS.md`、`共有仕様書.md`、既存ファイルを確認し、以下の仕様を実装してください。

# 出張旅費精算 共有仕様書

## 現在の状態

ver1の「行き先中心の簡単入力」とブラウザ内運賃台帳方式を公開済み。ver2の過去請求データ読込、実績マスタ、複数日連続確定、新規入力・修正、20行単位のExcel出力基盤を実装済み。2026-07-26にver3の表形式Enter入力、実績候補、ブラウザ内保存、過去ODS読込、現行社内ODS原本を複製する20行単位のODS出力を実装。実操作不具合を修正したSites Version 8を「Ver3実運用テスト版」として本番公開済み。

## 決定済み仕様

- 出張旅費精算機能
- Workは相談・仕様確定、Codexは実装・テスト・Git・公開を担当し、切り分けはエージェント側で判断する。
- 現在のタスクで実装可能なら自動的に実装へ進み、画面切り替えが必要な場合だけ利用者へ案内する。
- 予定OCRは機能を残すが、入力の主経路にはしない。
- スクリーンショットはクリップボード貼り付けで端末内に参考保存できるようにする。
- 行き先入力から出発駅、最寄駅、移動理由、経路、IC運賃をブラウザ内の登録情報から自動補完する。
- 駅すぱあと等の有料サービスおよび外部経路APIとは連携しない。
- 初回または運賃変更時は区間とIC料金を修正し、「再計算」「確定して登録」でブラウザ内運賃台帳へ保存する。
- 登録済みの同一区間は次回から自動計算し、登録日と利用回数を表示する。
- 最短入力欄にIC料金を表示し、登録済み区間は自動表示、未登録区間は追加前に直接入力できるようにする。
- 登録状況を月別一覧で確認し、確認済み行だけを書き出し・コピー出力できるようにする。
- ver3ではPCブラウザ向けの表形式画面を主画面とし、日付、目的地、区間、金額、理由をEnterで順に確定する。
- ver3では過去実績を候補表示し、矢印キーとEnterで選択できる。候補外の通常入力も許可する。
- ver3では設定情報、月データ、実績マスターをブラウザ内に保存する。
- ver3では定期区間情報を保持するが、旅費判定には使用しない。
- ver3の出力は現行社内フォーマット固定のODSとし、空白行と0円行を除外して20件ごとにページ分割する。
- ver3のファイル名は `YYYYMM_出張旅費精算書.ods` とし、合計金額はODS計算式を利用する。
- ODS作成後の確認、修正、印刷はLibreOfficeで行う。

## 未決事項

- Version 8次回改善項目：Windows版LibreOfficeでの表示確認、Windows実機印刷確認、設定画面整理。
- Version 9公開後確認項目：Windows版LibreOfficeでの表示、MSゴシック表示、Windows実機での印刷結果。
- Windows版の対応方針
- ver2で使用する前回のExcel出張旅費申請書原本と、読み込む過去請求データの実ファイル確認
- 過去データの読込列、実績マスタの項目、重複・複数候補・運賃改定時の扱い
- 複数日付の具体的な選択UI
- 40行を超える場合のシート追加ルール
- 過去ODS申請書の実ファイルによる読み込み確認
- 保存先再利用、同名ファイル上書き確認、File System Access API採用の要否。ver3公開の必須条件には含めず、将来の検討事項とする。
- 既存ver1／ver2の詳細機能をver3画面に残す範囲
- Windows版LibreOfficeでのMSゴシック、文字切れ、罫線、結合セル、印刷プレビューの実機確認

## 進行中の変更

- ver2・過去申告実績マスタ方式。外部路線検索を使わず、過去請求データの読込、実績選択、新規入力・修正、複数日連続確定、20行単位のExcelシート出力へ変更する。
- ver3・表形式入力／ODS出力方式。PCブラウザでEnter中心の連続入力、過去実績候補、実績マスター管理、過去ODS取込、現行社内書式固定のODS出力へ変更する。
- Version 7実操作不具合修正。表形式入力の直接編集とEnter移動、専用候補、原本固定ODS取込、異常金額補正を優先し、設定画面構成は変更しない。詳細は `docs/specs/2026-07-26-ver3-ods-entry-app.md` を正本とする。
- Phase 1メイン画面再構成。通常導線を「旅費入力」「設定」の2系統にし、ODS作成を旅費入力へ統合、設定を基本情報・過去データ取込・実績マスター管理・データ管理へ分ける。入力・候補・実績・ODS処理と既存データは変更しない。
- Phase 2-1初期実績マスター。保存済み状態が存在しない新規環境で、実績マスターが空の場合に限り正式な初期実績6件を `claimMasters` へ登録する。保存済み状態がある環境は、実績マスターが空の場合を含め、追加・上書きしない。初期実績は候補入力だけに使用し、`expenses` には登録しない。
- Phase 2-2過去ODS取込確認画面。既存の原本解析結果をローカルの編集可能一覧へ表示し、日付・目的地・区間・金額・理由の修正、登録対象外設定、行削除を可能にする。確認中は既存実績マスターへ反映せず、「実績マスターへ登録」確定時だけ対象行を安全な数値へ補正して登録する。

## 関連仕様

- [行き先中心入力・スクリーンショット保存仕様](docs/specs/2026-07-18-destination-first-entry.md)
- [外部連携なし・ブラウザ内運賃台帳仕様](docs/specs/2026-07-18-local-fare-ledger.md)
- [ver2・過去申告実績マスタ方式仕様](docs/specs/2026-07-19-ver2-past-claims-master.md)
- [ver3・表形式入力／ODS出力仕様](docs/specs/2026-07-26-ver3-ods-entry-app.md)

## 完了条件

- 対象機能のテストが完了している。
- 必要な場合はGitHubへのpushと公開確認が完了している。

## 変更履歴

- 2026-07-17：共有運用ルールを整備。
- 2026-07-18：OCRを補助機能へ変更し、スクリーンショット保存と行き先中心の自動補完・運賃計算を進行中仕様として追加。
- 2026-07-18：行き先中心入力、同日経路連結、履歴運賃の要確認表示、スクリーンショット貼り付け保存、駅すぱあと連携経路を実装。commit `5728358`。機能テスト11件、公開物テスト2件、型チェック、ビルドに成功。公開URLのHTTP 200を確認。
- 2026-07-18：WorkとCodexの自動切り分け方針を追加。相談はWork、実装・テスト・Git・公開はCodexとし、不要な画面切り替えを利用者へ求めない運用に変更。
- 2026-07-18：駅すぱあと等の外部経路APIを採用せず、ブラウザ内運賃台帳、再計算、確定登録、登録状況一覧へ変更する仕様を確定。
- 2026-07-18：ブラウザ内運賃台帳、再計算、確定登録、登録状況一覧を実装し、外部運賃APIを削除。commit `ecc110d`。機能テスト12件、公開物テスト2件、型チェック、公開用ビルドに成功。Sites version 4を公開し、公開URLのHTTP 200を確認。
- 2026-07-18：初回区間の金額入力箇所が分かりにくかったため、最短入力欄にIC料金の直接入力と登録運賃の自動表示を追加する仕様を確定。
- 2026-07-18：最短入力欄へIC料金を追加。登録済み金額の自動表示と初回金額の直接入力に対応。commit `8b84431`。機能テスト12件、公開物テスト2件、型チェック、ビルドに成功。Sites version 5を公開し、公開URLのHTTP 200を確認。
- 2026-07-19：ver2の大幅変更方針を仕様化。外部路線検索を廃止し、過去申告実績マスタ、過去請求データ読込、複数日連続確定、新規訪問先入力・修正、前回のExcel原本準拠、20行超過時の複数シート出力を進行中仕様へ追加。
- 2026-07-19：ver2基盤を実装。過去XLSX・CSV・TSV読込、確認後の実績マスタ登録、重複集約、複数日連続確定、新規入力・修正、確定日一覧、既存の20行単位複数シート出力へ接続。機能テスト14件、公開物テスト2件、型チェック、ビルドに成功。実原本と実データによる最終確認は継続。
- 2026-07-26：ver3仕様を確定。PC向け表形式Enter入力、過去実績候補、設定・月データ・実績マスターのブラウザ内保存、過去ODS取込、20件単位の固定ODS出力、LibreOfficeでの確認・修正・印刷へ変更する方針を採用。変更専用仕様書 `docs/specs/2026-07-26-ver3-ods-entry-app.md` を追加。
- 2026-07-26：ver3初期実装。表形式Enter入力、前回日付引継ぎ、過去実績候補、行完成時の実績更新、実績マスター編集・削除、ODS過去申請書読込、20件単位のODS複数シート出力、ページ番号・合計計算式を実装。機能テスト15件、公開物テスト2件、型チェック、公開用ビルドに成功。社内ODS原本への書式合わせとWindows／LibreOffice確認は継続。
- 2026-07-26：現行社内ODS原本を解析。使用シート `【原本】出張旅費精算`、明細11～30行、A/B/C/D:E/F/G:H列、所属C1:D1、氏名A5:D5、月度A3:H3、ページ番号G1:H1、合計F31、A4縦・指定余白・66%縮小の印刷設定を確定。原本複製型出力へ変更する。
- 2026-07-26：現行社内ODS原本対応を実装。原本シートを20件単位で複製し、セル値だけを設定する方式へ変更。21件で2シート、F31計算式、A4縦2ページ、原本書式・設定ファイルの同一性をLibreOfficeとXML解析で確認。commit `0a519e0` をGitHubへpushし、Sites Version 7を保存。検証環境にMSゴシックがないため、Windowsでの最終字形・印刷プレビュー確認は継続。本番公開は承認待ち。
- 2026-07-26：Version 7の公開保留を確定。保存先再利用と同名ファイル上書き確認は必須修正ではなく将来の検討事項へ変更し、Windows版LibreOfficeでの表示・印刷確認を公開判断の優先条件とした。
- 2026-07-26：利用者承認によりSites Version 7を「Ver3実運用テスト版」として本番公開。公開URLと固定ODS原本のHTTP 200を確認。Windows版LibreOffice表示とWindows実機印刷は未確認事項として残し、実利用で必要になった改善はVersion 8以降で対応する。
- 2026-07-26：Version 7の実操作で表形式入力の直接編集・Enter移動不具合を確認。専用候補、原本シート固定の過去ODS取込、NaN等の異常金額補正を含む修正仕様を確定。設定画面整理は後続対応とする。
- 2026-07-26：Version 7実操作不具合を修正。理由Enter後の次行フォーカス、日・金額の直接入力、専用候補の上下キー選択、候補外入力、原本固定ODS取込、異常金額補正を実装。実ブラウザで1件・10件連続入力と複数候補を確認し、機能テスト18件、公開物テスト2件、型チェック、ビルドに成功。実装commit `650ee61`。
- 2026-07-26：Sites Version 8を「Ver3実運用テスト版」として一般公開。次回改善項目はWindows版LibreOffice表示、Windows実機印刷確認、設定画面整理とする。
- 2026-07-26：Phase 1としてメイン画面とナビゲーションをVer3仕様へ再構成する方針を確定。処理とデータ構造を維持し、通常導線だけを整理する。
- 2026-07-26：Phase 1メイン画面再構成を実装。旅費入力へ表入力とODS作成を集約し、設定を4区分化。旧機能と既存データを維持したまま通常導線から外した。ブラウザ操作確認、機能テスト18件、公開物テスト2件、型チェック、ビルドに成功。実装commit `6dd67e4`。
- 2026-07-26：Phase 2-1初期実績マスター仕様を確定。新規環境だけに正式な初期実績6件を登録し、既存環境には追加・上書きしない。入力明細は空のまま維持し、初期実績を候補検索・入力補助だけに使用する。
- 2026-07-26：Phase 2-1初期実績マスターを実装。保存状態の有無で新規・既存を分岐し、新規環境だけに使用回数1・初期登録日付きの6件を登録。新規ブラウザで空の入力行、候補6件、上下キー・Enter選択、入力行修正後のマスター非変更を確認。機能テスト20件、公開物テスト2件、型チェック、ビルドに成功。実装commit `73603c9`。Version公開は未実施。
- 2026-07-26：Phase 2-2過去ODS取込後の編集可能確認画面仕様を確定。ODS解析処理を維持し、登録前の編集・対象外設定・削除と、確定時だけの実績マスター反映を追加する。
- 2026-07-26：Phase 2-2過去ODS取込後の編集可能確認画面を実装。原本形式ODSの3行を実ブラウザで読み込み、日付・目的地・区間・金額・理由の編集、登録前のマスター非変更、対象外設定、行削除、編集済み1行の確定登録と実績反映を確認。機能テスト22件、公開物テスト2件、型チェック、ビルドに成功。実装commit `64fa355`。Version公開は未実施。
- 2026-07-26：公開前受入テストを実施。新規環境、初期候補、補完後修正、Enterによる10件連続入力、過去ODSの編集・対象外・削除・登録、現行ODS作成、LibreOffice 25.8でのA4縦印刷表示、罫線、結合セル、ページ番号、合計式を確認。機能テスト22件、公開物テスト2件も成功し、Version 9の一般公開を承認。Windows版LibreOffice、MSゴシック、Windows実機印刷は公開後確認項目とする。
