# ver3・表形式入力／ODS出力仕様

## 文書情報

- 対象アプリ：出張旅費申請アプリ
- 開発仕様書版：Ver.1.0
- 採用日：2026-07-26
- 位置付け：既存ver2からver3へ移行するための変更専用仕様書

## 目的

社内出張旅費申請を、過去実績を利用した表形式入力と現行社内書式のODS出力により簡略化する。

## 変更提案（承認済み）

### 変更内容

- 主画面を、日付・目的地・区間・金額・理由を行単位で入力するPC向け表形式へ変更する。
- 入力は各項目をEnterで確定し、次の項目、次の行へ進む。
- 過去実績を候補として表示し、矢印キーとEnterで選択できるようにする。
- 出力形式を、Excel中心の出力から現行社内フォーマット固定のODS出力へ変更する。
- 保存データを設定情報、月データ、実績マスターとして整理し、ブラウザ内に保存する。
- WindowsのPCブラウザを主利用環境とし、Mac等でも確認できるPWA構成を維持する。

### 変更理由

- 毎月の定型入力を、実際の申請書に近い順序で素早く処理するため。
- 過去実績を候補として再利用しつつ、候補外の入力も妨げないため。
- 利用者が最終確認・修正・印刷にLibreOfficeを使用できるODSファイルを作るため。

### メリット

- キーボード中心で連続入力できる。
- 実績を使うほど候補精度と入力効率が上がる。
- クラウド、ログイン、外部経路APIに依存せず個人利用できる。
- 現行社内書式に合わせた出力を維持しやすい。

### デメリット

- ODS原本の実ファイルと印刷結果を使った最終調整が必要。
- 書式変更時はアプリ改修が必要。
- ブラウザの安全制約により、保存先の再利用や同名ファイルの上書き確認は対応ブラウザの機能差を受ける。
- 既存ver1／ver2画面とデータからの移行処理が必要。

## 変更内容

### 1. 開発方式と利用範囲

- PWA（Webアプリ）として実装する。
- PCブラウザ、主にWindows環境で利用する。
- Mac等でも確認可能な構成とする。
- 個人利用を前提とし、ログイン、クラウド同期、複数端末同期は実装しない。

### 2. データ保存

- ブラウザ内保存を使用する。実装方式はIndexedDBを基本とする。
- 設定情報として、所属、氏名、定期区間情報を保存する。
- 定期区間は情報保持のみとし、旅費判定には使用しない。
- 月単位で、日付、目的地、区間、金額、理由を保存する。
- 実績マスターとして、目的地、区間、金額、理由、使用回数、最終利用日を保存する。

### 3. メイン画面

- 表示列は「日付、目的地、区間、金額、理由」とする。
- 1項目ごとにEnterで確定し、日付 → 目的地 → 区間 → 金額 → 理由 → 次行の日付の順に進む。
- 対象月を選択し、日付欄には日だけを入力できる。
- 日・金額は入力途中の文字列をそのまま表示し、Enterまたはフォーカス移動時に数値へ確定する。半角数字、全角数字、全角カンマ付き金額を受け付け、入力途中で既定値1、0、空欄へ強制変換しない。
- 次行の日付欄には直前行の日付を初期表示する。
- 対象月は空欄にできない申請単位の管理情報とし、通常時は「YYYY年M月 出張旅費入力」と表示する。月変更は専用操作で有効な年月を確定した場合だけ反映する。
- 空白行と金額0円の行は出力対象外とする。

### 4. 候補入力

- 過去実績を入力候補として利用する。
- 目的地欄では、実績マスターの目的地を重複なしの候補一覧として表示する。文字入力は候補の絞り込みに利用できるが必須とはしない。
- 目的地候補は上下キーまたはクリックで選択し、選択した場合だけ目的地へ反映する。この時点では区間、金額、理由を補完しない。
- 目的地選択後、同じ目的地を持つ実績マスターを区間候補として一覧表示する。
- 区間候補には区間、金額、理由を表示し、上下キーまたはクリックで明示選択した候補だけを区間、金額、理由へ反映する。
- 第一候補の自動選択・自動入力は禁止する。補完後の区間、金額、理由は自由に修正できる。
- 候補がない場合は通常入力として扱い、入力を制限しない。

### 5. 実績マスター更新

- 1行の入力が完了した時点で実績マスターを更新する。
- 目的地、区間、金額、理由が完全一致する場合は、使用回数を1増やし、最終利用日を更新する。
- 既存候補から内容を変更した場合は、新しい実績として登録する。
- マスター登録には、最低限、目的地と区間を必要とする。

### 6. 設定画面

- 基本情報：所属、氏名、定期区間情報。
- 過去申請書取り込み。
- 実績マスター管理。
- データ移行。
- 実績マスター管理では、目的地、区間、金額、理由、使用回数、最終利用日を表示し、編集・削除できる。

### 7. 過去申請書取り込み

- ODSを読み込み、明細を解析して実績マスターを更新する。
- 日付、目的地、区間、金額、理由が完全一致する明細は重複追加しない。
- 取り込みに必要な実際のODSセル配置は、現行原本を確認して確定する。

### 8. ODS出力

- 現行社内フォーマット固定とし、利用者によるテンプレート変更機能は実装しない。
- 書式変更時はアプリ改修で対応する。
- 固定原本は `2026年度版出張旅費代精算書原本（エクセル入力）.ods` とする。
- 使用シートは `【原本】出張旅費精算` とし、`【見本】出張旅費精算` は出力しない。
- 原本のシートをページ数分複製し、書式、罫線、結合セル、注意書き、印刷設定を維持したまま値だけを設定する。
- 日付の月・日、目的地、区間、金額、理由を出力する。
- 月と日は数字として出力する。
- 明細は11～30行とし、A列へ月、B列へ日、C列へ目的地、D:E結合セルへ区間、F列へ金額、G:H結合セルへ理由を出力する。
- 所属はC1:D1結合セル、氏名はA5:D5結合セル、月度見出しはA3:H3結合セル、ページ番号はG1:H1結合セルへ出力する。
- 合計欄はF31とし、各シートで `SUM(F11:F30)` を使用する。
- 明細を20件ごとに分割し、ページ番号を自動入力する。
- 合計金額はアプリで固定値を書かず、ODS計算式を利用する。
- 出力シート名は `出張旅費精算_1` から始め、20件ごとに連番を増やす。
- 原本の印刷設定（A4縦、上下余白0.762cm、左右余白1.778cm、66%縮小）を維持する。
- ファイル名は `YYYYMM_出張旅費精算書.ods` とする。
- ODSはブラウザ標準のダウンロード機能で保存する。
- 保存先の再利用と同名ファイルの上書き確認は、ブラウザ制約と対応状況を踏まえた将来の検討事項とし、ver3の公開必須条件には含めない。
- ODS作成後の確認、修正、印刷はLibreOfficeで行う。

### 9. 初期テストデータ

| 区分 | 目的地 | 区間 | 金額 | 理由 |
|---|---|---|---:|---|
| メイン | 本社 | 武蔵浦和→北与野 | 199円 | 本社業務 |
| メイン | 南越谷 | 北与野→武蔵浦和 | 199円 | 教室管理 |
| メイン | 越谷レイクタウン | 北与野→武蔵浦和 | 199円 | 教室管理 |
| 時々利用 | 川越教室 | ふじみ野→川越 | 178円 | 巡回 |
| 時々利用 | 本社 | 川越→北与野 | 341円 | 本社業務 |
| 追加 | 自宅 | 北与野→武蔵浦和 | 199円 | 帰宅 |

## 変更しない範囲

- クラウドサービス、ログイン、複数端末同期は追加しない。
- 定期区間情報を旅費額の自動判定には使用しない。
- 外部経路検索や外部AIへ申請データを送信しない。
- ODS作成後の確認、修正、印刷はアプリ内で行わない。
- 既存の保存データを利用者の許可なく削除しない。

## 実装計画・作業項目

### Ver3段階候補選択

#### 目的

- 目的地と区間の選択を分け、同じ目的地に複数区間がある場合でも利用者が意図した実績だけを反映できるようにする。

#### 変更内容

- 目的地候補は実績マスターの目的地を重複なしで表示し、空欄フォーカス時にも選択できる。
- 目的地候補の選択では目的地だけを反映し、区間・金額・理由を空欄のまま維持する。
- 区間欄へ、その目的地の実績候補を区間・金額・理由付きで表示する。
- 区間候補を明示選択した場合だけ区間・金額・理由を反映する。
- 目的地・区間の直接入力、候補選択後の金額・理由の直接修正は維持する。

#### 変更しない範囲

- 入力途中の文字列保持、日・金額の全角数字対応、行完成時の実績マスター更新、初期実績6件、過去ODS解析、ODS生成、月管理は変更しない。

#### 実装・検証結果（2026-07-27）

- 新規IndexedDB環境の空の目的地欄で、本社、南越谷、越谷レイクタウン、川越教室、自宅の5目的地を重複なしで表示した。
- ArrowDownとEnterで本社を選択し、目的地だけが反映され、区間・金額・理由は未入力のまま維持された。
- 本社選択後、武蔵浦和→北与野・199円・本社業務と、川越→北与野・341円・本社業務の2区間候補を表示した。
- 2件目の川越→北与野を明示選択した場合だけ341円と本社業務が反映された。
- 補完後の金額を全角「３５０」へ直接修正し、350円として行完成・次行生成・合計反映を確認した。
- クリック操作でも南越谷を選択した時点では金額が空欄のまま区間候補を表示し、区間候補のクリック後だけ199円と教室管理を反映した。
- 機能テスト27件、公開物テスト2件、型チェック、公開用ビルドに成功した。一般公開は行っていない。

### Ver3入力機能安定化・候補選択・月管理改善

#### 目的

- 日・金額の入力途中表示を維持し、日本語入力環境でも毎月の旅費明細を安定して完成できるようにする。
- 実績候補を補助機能に限定し、利用者が明示選択した候補だけを反映する。
- 対象月を空欄化できない申請単位情報として扱う。

#### 変更内容

- 日・金額は画面内の入力途中文字列として保持し、Enterまたはフォーカス移動時だけ明細データへ確定する。
- 数値確定時にUnicode正規化を行い、半角数字、全角数字、全角カンマ付き金額を有限の0以上の数値として保存する。
- 単一候補を含め、目的地入力・フォーカスだけでは候補を選択状態にしない。上下キーまたはクリック後のEnterだけで候補を反映する。
- 対象月は年月見出しとして表示し、専用の月変更操作で有効な年月を確定した場合だけ切り替える。
- 旧保存データに対象月の空欄・不正値がある場合は、読み込み時に現在月へ安全補正する。
- 対象月が有効でない場合の警告とODS作成禁止条件を追加する。

#### 変更しない範囲

- IndexedDBの保存構造、実績マスターの一致・利用回数更新、初期実績6件、過去ODS解析、現行原本ODS生成、20件分割、合計式は変更しない。
- Ver1／Ver2の通常導線外機能は削除しない。

#### 実装・検証結果（2026-07-27）

- 新規IndexedDB環境で全角日「２」が入力中にそのまま表示され、Enter確定後は日「2」として保持された。
- 全角金額「１９９」が入力中にそのまま表示され、候補補完後に全角「２００」へ修正して200円として保存できた。
- 「本社」の2候補を表示し、通常Enterでは目的地だけを保持して区間・金額・理由を空欄のまま次へ移動した。ArrowDownとEnterで選んだ候補だけを補完した。
- 理由Enter後に日「2」を引き継いだ2行目を生成し、日欄へフォーカスした。
- 初期実績6件と、修正後の200円実績を含む計7件をマスター管理画面で確認した。初期6件はすべて目的地検索の対象だった。
- 完成1行・200円がODS出力対象になり、ODS作成ボタンから現行原本形式のファイルを作成した。
- 月変更欄を空にしても表示中の「2026年7月 出張旅費入力」を保持し、確定ボタンを無効化して説明を表示した。
- 機能テスト26件、公開物テスト2件、型チェック、公開用ビルドに成功した。実装commitは `842fe9e`。一般公開は行っていない。

### Phase 2-2：過去ODS取込後の編集可能確認画面

- 既存のODS原本シート解析、11～30行解析、見本シート除外、月・日・目的地・区間・金額・理由の取得処理は変更しない。
- 解析結果を、日付・目的地・区間・金額・理由の編集可能な表形式一覧へ一時保持する。
- 各行は登録対象・対象外を切り替えられ、不要行を一覧から削除できる。
- 編集中のデータは画面内の一時状態とし、元ODSファイルと既存実績マスターを変更しない。
- 「実績マスターへ登録」の確定時だけ登録対象行を反映する。
- 金額は編集時と登録時に安全な有限数値へ補正し、NaN、Infinity、文字列を実績マスターへ保存しない。
- 完全一致は既存の集約処理により使用回数を加算し、最終利用日を更新する。新規内容は新しい実績として登録する。
- 旅費入力、Enter入力、候補一覧、初期実績マスター、ODS出力、原本解析は変更しない。

完了条件：

- 実ブラウザでODSを読み込み、解析結果の表示と全5項目の編集ができる。
- 対象外設定と行削除ができ、対象行だけを実績マスターへ登録できる。
- 登録操作前は既存実績マスターの件数・内容が変わらない。
- 登録後、編集内容、完全一致の利用回数、最終利用日、新規実績が正しく反映される。
- 機能テスト、公開物テスト、型チェック、ビルドに成功する。

実装・検証結果（2026-07-26）：

- ODS解析結果を画面内の一時状態へ保持し、日付・目的地・区間・金額・理由を直接編集できる一覧へ変更した。
- 各行へ「登録対象／対象に戻す」と「削除」を追加し、登録対象件数を確定前に確認できるようにした。
- 原本形式の検証用ODSから3行を読み込み、5項目すべてを編集できることを実ブラウザで確認した。
- 登録前は初期実績6件のままであることを確認後、1行を対象外、1行を削除し、編集済み1行だけを確定登録した。

### Version 9公開版 実操作不具合修正

#### 目的

- 公開版に残る旧保存データとVer3画面の組み合わせを含め、日常の表入力と過去ODS取込をVer3仕様どおり成立させる。

#### 変更内容

- 保存済み明細に不足するVer3用の行状態・作成日時等を読み込み時に安全に補完する。旧版が自動生成した未入力行だけは日付1・区間等を空へ戻し、利用者が入力した完成明細や既存実績マスターは削除・上書きしない。この移行は保存形式Version 2への更新時に1回だけ行う。
- 日付欄は空欄を許容したまま利用者の入力値を保持し、完成行の次行だけ直前の日を引き継ぐ。
- 新規行の区間等を入力済みに見せる例示文字を表示しない。目的地候補を選択した場合だけ区間・金額・理由を補完する。
- 行追加と再描画の完了後に保留中のフォーカス移動を再試行し、理由Enter後に次行の日へ移動する。
- 候補反映を含む行編集を未確認状態に戻し、完成確定時に実績マスターを更新する。
- 過去ODSは `【原本】出張旅費精算` とその追加ページを対象に、各シート11～30行のA・B・C・D・F・G列を読む。`【見本】出張旅費精算` は対象外とする。
- 申請書で省略される連続明細の月・日は直前の有効値を引き継ぐ。
- ODS内部の `text:ruby-text` はふりがな注釈として除外し、画面に表示される本文だけを取り込む。

#### 変更しない範囲

- 現行ODS原本を利用する出力処理、20件分割、ページ番号、合計式は変更しない。
- 初期実績6件は保存状態が存在しない新規環境だけに生成し、保存済み環境の既存マスターへ追加・上書きしない。
- 入力明細と実績マスターは分離したまま維持する。

#### 完了条件

- 新規環境で空の入力行と初期実績候補6件を確認できる。
- 日を1以外へ変更して保存後も保持される。
- 候補外入力と候補選択の双方で、理由Enter後に3行以上連続して次行へ進める。
- 完成行が実績マスターへ新規登録され、完全一致は使用回数と最終利用日を更新する。
- 実在する複数明細・複数原本ページのODSを読み、月日省略行を含む全明細を日本語本文だけで確認一覧へ表示できる。
- 自動テストに加え、ブラウザで利用者操作シナリオを再確認する。

#### 実装・検証結果

- 日付欄は「2」へ変更後も再描画で保持され、完成行の次行へ「2」、続く入力では「3」「4」が引き継がれた。
- 新規行の区間・金額・理由は空欄で、目的地候補を選んだ行だけが補完された。
- 理由Enterで2行目から3行目、3行目から4行目の日付へフォーカスが移動した。
- 初期実績6件を持つ新規ブラウザで3行を完成し、実績マスターが9件になったことを管理画面で確認した。
- 実在する2025年度4月ODSは省略月日を含む2行、同6月の2ページODSは21行を取得した。ふりがな注釈を除外し、「浦和高校」「さいたま新都心→北浦和」「打合せ」等の画面本文だけを確認一覧へ表示した。
- 機能テスト25件と公開物テスト2件、型チェック、公開用ビルドに成功した。
- 実績マスターに「編集後訪問先／新宿→渋谷／321円／編集後理由／2026-07-25」が7件目として反映された。
- 完全一致の利用回数・最終利用日更新、対象外行の除外、NaN・Infinityの0円補正を自動テストで確認した。
- 機能テスト22件、公開物テスト2件、型チェック、ビルドに成功した。Version公開は行っていない。

### Phase 2-1：初期実績マスター

- 保存済み状態が存在しない新規ブラウザ環境で、実績マスターが空の場合に限り、初期テストデータ6件を正式な初期実績として `claimMasters` へ登録する。
- 保存済み状態が存在する場合は、`claimMasters` が空の場合を含め、初期実績を追加・上書きしない。
- 初期実績は候補検索・入力補助だけに使用し、入力明細 `expenses` には登録しない。
- 新規環境の空入力行は日付を未入力表示とし、初期実績を理由とした日付1、区間、金額等の自動表示を行わない。入力済み行から次行への日付引継ぎは維持する。
- 使用回数は1、最終利用日は初期登録日、登録元は「初期実績」とする。
- 候補を入力行へ反映した後に利用者が行を修正しても、行完了前は初期実績マスター自体を変更しない。
- データ構造、入力表ロジック、候補選択ロジック、ODS処理は変更しない。

完了条件：

- 新規環境の旅費入力画面は既存仕様どおり空の入力行から始まり、初期実績6件が目的地候補として表示される。
- 候補選択により目的地、区間、金額、理由が入力行へ反映される。
- 入力行の編集が初期実績マスターへ波及しない。
- 保存済み状態にある既存実績マスターが追加・上書きされない。
- 自動テスト、型チェック、ビルド、実ブラウザ操作確認に成功する。

実装・検証結果（2026-07-26）：

- 保存状態がない場合だけ初期実績6件を生成する起動分岐を実装した。保存済み状態は、実績マスターが空の場合も含めてそのまま使用する。
- 新規ブラウザ環境で、日付・目的地・区間・金額・理由が未入力の空行と、目的地フォーカス時の初期候補6件を確認した。
- 上下キーとEnterで「本社」を選択し、区間、199円、理由が入力行へ反映されることを確認した。
- 入力行の理由を変更した後も、実績マスターの「本社／本社業務」が変更されず、登録件数が6件のままであることを確認した。
- 機能テスト20件、公開物テスト2件、型チェック、ビルドに成功した。Version公開は行っていない。

### Phase 1：メイン画面・ナビゲーション再構成

- 通常のメインナビゲーションを「旅費入力」「設定」の2系統にする。
- 起動時は「旅費入力」を表示し、対象月、表形式入力、入力件数・合計金額、ODS申請書作成を同じ作業画面へ集約する。
- 独立したODS出力タブを廃止し、入力完了後の操作として旅費入力画面内へ配置する。
- 設定配下を「基本情報」「過去データ取込」「実績マスター管理」「データ管理」に分ける。
- 基本情報では所属、氏名、定期区間情報を扱い、データ管理ではバックアップと復元を扱う。
- 旧経路管理、曜日ルール、旧予定取込、旧コピー出力、旧Excel出力の実装と保存データは削除せず、通常導線から外す。
- 入力表ロジック、Enter移動、候補一覧、実績マスター処理、ODS解析、ODS生成、データ構造は変更しない。

完了条件：

- 起動直後に旅費入力画面が表示される。
- 上部の通常ナビゲーションが「旅費入力」「設定」の2項目になる。
- 旅費入力画面から入力後にODS申請書を作成できる。
- 設定内の4区分へ切り替えられる。
- 既存の入力・候補・ODS関連テストが維持され、ブラウザ操作確認とビルドが成功する。

### Version 7 実操作不具合修正

- 日・目的地・区間・金額・理由をクリックして直接編集でき、Enterで次項目へ確実に移動する。
- 理由欄のEnterで行を確定し、直前の日を引き継いだ次行を表示して日欄へフォーカスする。
- 10件程度を連続入力しても、行追加とフォーカス移動が停止しないようにする。
- ブラウザ標準候補を廃止し、目的地・区間・金額・理由を表示する専用候補を使用する。
- 候補は上下キーとEnterで選択でき、候補を選択しない通常入力を強制置換しない。
- 過去ODSは `【原本】出張旅費精算` の11～30行、A・B・C・D・F・G列だけを読み、`【見本】出張旅費精算` を除外する。
- 保存・復元・編集時の金額は有限の0以上の数値へ補正し、NaN・Infinity・数値以外を保存しない。
- 設定画面の構成は今回変更しない。

完了条件：

- 実ブラウザで1件入力、10件連続入力、候補選択、候補外入力を確認できる。
- 現行原本形式の過去ODSから目的地・区間・金額・理由を取り込める。
- 見本シートと空白行を取り込まない。
- 不正金額を含む既存データが安全に補正され、実績マスターにNaNを表示しない。
- 現行ODS出力と20件単位のページ処理を維持する。

### Phase 1：基本入力

1. ver3データ形式と既存データ移行を実装する。
2. 対象月と表形式入力画面を実装する。
3. Enter移動、日だけの入力、前回日付の引継ぎを実装する。
4. 月データのブラウザ内保存を実装する。

### Phase 2：実績活用

1. 入力候補、矢印キー選択、Enter確定を実装する。
2. 行完成時の実績マスター更新を実装する。
3. 実績マスターの編集・削除を実装する。
4. ODS過去申請書の読み込みと重複除外を実装する。

### Phase 3：ODS出力

1. 現行ODS原本のシート、セル、数式、印刷範囲を確認する。
2. 固定書式への明細出力を実装する。
3. 20件単位のページ分割、ページ番号、合計式を実装する。
4. ファイル名、保存先、同名ファイル確認を実装する。
5. LibreOfficeで表示・数式・印刷結果を確認する。

### Phase 4：補助機能

1. 設定画面とデータ移行を整備する。
2. Windowsでのキーボード操作と保存操作を確認する。
3. UIとアクセシビリティを改善する。

## 未決事項・必要資料

- Version 8次回改善項目：Windows版LibreOfficeでの表示確認、Windows実機印刷確認、設定画面整理。
- 過去ODS申請書の実ファイルによる読み込み確認。
- 保存先再利用、同名ファイル上書き確認、File System Access API採用の要否。
- 既存ver1／ver2の詳細機能をver3画面に残す範囲。
- Windows版LibreOfficeでのMSゴシック、文字切れ、罫線、結合セル、印刷プレビューの実機確認。

## 完了条件

- 対象月を選び、日付、目的地、区間、金額、理由をEnter中心で連続入力できる。
- 入力内容が月単位でブラウザ内に保存される。
- 過去実績候補をキーボードで選択でき、候補外も自由入力できる。
- 行完成時に実績マスターが仕様どおり更新される。
- 実績マスターを表示、編集、削除できる。
- 過去ODS申請書を読み込み、重複を除いて実績マスターへ反映できる。
- 空白行と0円行を除き、20件単位で現行書式のODSを作成できる。
- ODSのファイル名、ページ番号、合計式が仕様どおりである。
- LibreOfficeで確認、修正、印刷できる。
- 対象機能のテストとビルドが完了し、既存データが失われない。

## 実装結果

- 2026-07-26にver3の初期実装を実施。
- 既存のIndexedDB保存とver1／ver2データを維持したまま、表形式入力を既定画面として追加。
- 日、目的地、区間、金額、理由のEnter移動、目的地候補、候補選択時の区間・金額・理由補完、次行への日付引継ぎを実装。
- 行完成時の月データ保存、実績マスターの完全一致集約、使用回数・最終利用日更新を実装。
- 実績マスターの一覧、編集、削除を実装。
- ODS・XLSX・CSV・TSVの過去申請書取り込み導線を実装。ODSは内部の表データを端末内で解析する。
- 空白行と0円行を除外し、20件単位で複数シートを作成するODS出力を実装。
- ODS内に所属、氏名、ページ番号、明細、シート単位の合計計算式を出力し、ファイル名を `YYYYMM_出張旅費精算書.ods` とした。
- 機能テスト15件、公開物テスト2件、TypeScript型チェック、公開用ビルドに成功。
- 2026-07-26に現行社内ODS原本対応を実装。固定原本をアプリへ同梱し、`【原本】出張旅費精算` シートを20件単位で複製して値だけを書き換える方式へ変更。
- 原本のセル位置、結合セル、罫線、文字スタイル、注意書き、A4縦・指定余白・66%縮小の印刷設定を維持する。
- 21件の検証データで2シートを生成し、LibreOfficeで修復警告なく開けること、PDF変換時にA4縦2ページとなることを確認。
- 1ページ目20件・F31合計3,980円、2ページ目1件・F31合計199円となり、各シートの `SUM(F11:F30)` が正しく動作することを確認。
- 出力ODSと原本の `styles.xml`、`settings.xml`、`META-INF/manifest.xml` が同一であることをSHA-256で確認。
- 検証環境に原本指定のMSゴシックがないため、PDF上の日本語字形表示は確認できなかった。セル内の日本語文字列と配置はXMLで確認済み。Windows／LibreOfficeでの最終字形・印刷プレビュー確認は継続。
- 保存フォルダ再利用、同名ファイル上書き確認は未実施。
- ODS原本対応の実装commitは `0a519e0`。GitHubの `main` へpush済み。Sites Version 7を「Ver3実運用テスト版」として本番公開済み。
- 2026-07-26：Version 7は公開せず保留。保存先再利用と同名ファイル上書き確認はブラウザ制約を踏まえた検討事項とし、Windows版LibreOfficeでの表示・印刷確認を公開判断の優先条件とする。
- 2026-07-26：利用者承認によりVersion 7の保留を解除し、「Ver3実運用テスト版」として本番公開。公開URLと固定ODS原本のHTTP 200を確認。Windows版LibreOffice表示とWindows実機印刷は未確認事項として残し、必要な改善はVersion 8以降で対応する。
- 2026-07-26：Version 7実操作不具合を修正。行追加後の画面描画とフォーカス移動の順序を是正し、理由Enter後に次行の日へ確実に移動するよう変更。日・金額の直接上書き、専用候補の上下キー選択、候補外入力、原本シート11～30行固定の過去ODS取込、不正金額の0円補正を実装。実ブラウザで1件入力、10件連続入力、同一目的地の複数候補選択、候補外入力を確認。機能テスト18件、公開物テスト2件、型チェック、公開用ビルドに成功。実装commit `650ee61`。
- 2026-07-26：Sites Version 8を「Ver3実運用テスト版」として一般公開。次回改善項目はWindows版LibreOffice表示、Windows実機印刷確認、設定画面整理とする。
- 2026-07-26：Phase 1メイン画面再構成を実装。通常ナビゲーションを旅費入力・設定の2系統に変更し、入力表とODS作成を同一画面へ配置。設定を基本情報・過去データ取込・実績マスター管理・データ管理へ分割した。旧機能と既存データは削除せず通常導線から外した。ブラウザで起動時入力画面、ODS作成導線、設定4区分、旧メニュー非表示を確認。機能テスト18件、公開物テスト2件、型チェック、ビルドに成功。実装commit `6dd67e4`。
